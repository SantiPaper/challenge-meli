import { styled } from "styled-components";

export const StyledHome = styled.div`
  text-align: center;
  .hero {
    padding: 1rem;
    margin-top: 5rem;
    font-size: var(--font-size-600);
  }
`;
